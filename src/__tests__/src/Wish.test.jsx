// ********RoostGPT********
// Test generated by RoostGPT for test ReactTesting using AI Type Open AI and AI Model gpt-4-1106-preview



// ********RoostGPT********
import React from 'react';
import { render, screen } from '@testing-library/react';
import Wish from '../../Wish.jsx';

describe('Wish component', () => {
  test('displays the birthday message with capitalized name', () => {
    render(<Wish name="John" />);
    const messageElement = screen.getByText(/happy birthday john/i);
    expect(messageElement).toBeInTheDocument();
    expect(messageElement).toHaveClass('wish-message');
    expect(screen.getByText('JOHN')).toHaveClass('highlight');
  });

  test('throws an error when rendered without a name prop', () => {
    // We're using a console.error mock to suppress error output during the test
    const consoleError = jest.spyOn(console, 'error');
    consoleError.mockImplementation(() => {});

    expect(() => render(<Wish />)).toThrowError();

    // Restore the original console.error
    consoleError.mockRestore();
  });
});

