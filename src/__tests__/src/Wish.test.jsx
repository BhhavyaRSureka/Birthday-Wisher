// ********RoostGPT********
// Test generated by RoostGPT for test ReactTesting using AI Type Open AI and AI Model gpt-4-1106-preview



// ********RoostGPT********
import React from 'react';
import { render } from '@testing-library/react';
import Wish from '../../Wish.jsx';

describe('Wish Component', () => {

  test('renders the correct message with name prop', () => {
    const { getByText } = render(<Wish name="John" />);
    const wishMessage = getByText(/happy birthday JOHN !!!/i);
    expect(wishMessage).toBeInTheDocument();
  });

  test('renders the correct message when name prop is empty', () => {
    const { getByText } = render(<Wish name="" />);
    const wishMessage = getByText(/happy birthday !!!/i);
    expect(wishMessage).toBeInTheDocument();
  });

  test('renders the correct message with different case name prop', () => {
    const { getByText } = render(<Wish name="Jane" />);
    const wishMessage = getByText(/happy birthday JANE !!!/i);
    expect(wishMessage).toBeInTheDocument();
  });

  test('ignores additional props not defined in component', () => {
    const { getByText } = render(<Wish name="Dave" age={32} />);
    const wishMessage = getByText(/happy birthday DAVE !!!/i);
    expect(wishMessage).toBeInTheDocument();
    expect(() => getByText(/32/i)).toThrow();
  });

  // Add more tests here as needed to cover other scenarios and edge cases.
});


